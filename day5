#include <stdio.h>

int main() {
    int p, q;
    int A[100], B[100];
    int i = 0, j = 0;

    scanf("%d", &p);
    for(int k = 0; k < p; k++) {
        scanf("%d", &A[k]);
    }

    scanf("%d", &q);
    for(int k = 0; k < q; k++) {
        scanf("%d", &B[k]);
    }

    // Merge process
    while(i < p && j < q) {
        if(A[i] <= B[j]) {
            printf("%d ", A[i]);
            i++;
        } else {
            printf("%d ", B[j]);
            j++;
        }
    }

    // Remaining elements
    while(i < p) {
        printf("%d ", A[i]);
        i++;
    }

    while(j < q) {
        printf("%d ", B[j]);
        j++;
    }

    return 0;
}
